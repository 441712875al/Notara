# 更新日志

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 规范，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)（SemVer）。

## [0.1.0] - 2026-09-25

首个正式发布版本。

### 新增

- **即时渲染编辑器**：基于 vditor 的 IR（Instant Rendering）模式，光标所在行显示 Markdown 源码，其余行实时渲染为富文本，体验对齐 Typora。
- **工作区与多标签页**：单窗口管理一个文件夹（文件树 + 多标签页）；单独打开 `.md` 文件时退化为无文件树的轻量窗口。
- **文件树操作**：新建 / 重命名 / 删除（移入废纸篓），自动补全 `.md` 扩展名。
- **自动保存**：停止输入 500ms 后防抖写盘，切换标签、窗口失焦、关闭标签、退出应用时立即落盘。
- **外部变更检测**：实时监听文件系统，磁盘内容变化 / 文件被删除时分别给出「重载 / 冲突确认 / 标记已删除」的对应处理。
- **图片粘贴与拖拽**：截图或复制图片后直接粘贴 / 拖入，自动落盘至笔记同目录 `assets/` 并插入相对路径引用。
- **导出 HTML / PDF**：HTML 导出内联样式与图片，断网可看；PDF 导出 A4 分页，适合打印与存档。
- **主题系统**：跟随系统 / 亮色 / 暗色三档内置主题，支持自定义 CSS 主题。
- **Typora 主题导入**：将 Typora 主题包转换为 Notara 可用主题，自动桥接 CSS 变量、重写选择器、迁移字体图片等资源。
- **代码块就地编辑与高亮**：代码块可在原位展开编辑，渲染态叠加语法高亮，并提供一键复制按钮。
- **大纲面板**：列出当前文档标题层级，点击跳转定位。
- **欢迎页与最近工作区**：所有标签关闭后显示欢迎页，最近 10 个工作区按使用时间排序，一键重开。
- **窗口状态持久化**：退出时记录工作区、打开的标签、激活标签与窗口位置尺寸，下次启动自动恢复；侧栏宽度记忆。
- **退出数据安全**：退出 / 关闭前未保存内容强制落盘，无法自动保存的标签逐个引导另存为，写盘校验失败时中止退出。

### 变更

- 界面视觉重设计，采用「被照亮的纸」书页式排版，完善暗色模式细节。
- 侧边栏支持拖拽调宽与收起，宽度记忆持久化。

### 修复

- 修复顶栏无法拖动、双击无法缩放的问题（标签容器不再吞掉拖拽区域）。
- 修复代码块折叠态前方出现的空白矩形。
- 修复标签栏左侧避让红绿灯及红绿灯垂直居中问题。
- 修复 IPC 错误码跨 contextBridge 传递丢失的问题（改经 message 前缀编码）。
